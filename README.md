# onemc

A simple discord bot to start, stop and check the status of a minecraft server hosted on [AWS](https://aws.amazon.com/) using the [Crafty Controller](https://craftycontrol.com/) dashboard.
It utilizes the crafty control API, to manage the minecraft server state, and the AWS Go SDK for managing the EC2 instance state.
